use scene::light::Lighting;
use model::{SceneObject, Ray};
use model::intersect::Intersection;
use util::Color;

//pub fn trace(ray : &Ray, scene : &SceneObject, lightings : &Lighting) -> Option<Color> {}

//pub fn lighting_model(intersection : &Intersection, lightings : &Lighting) -> Color {}
